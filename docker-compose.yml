services:
  app:
    build: .
    ports:
      - '3000:3000'
    env_file: .env
    volumes:
      - linkyard-data:/data
    healthcheck:
      test: [CMD, curl, -f, 'http://localhost:3000/api/health']
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  linkyard-data:
